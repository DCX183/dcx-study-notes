### / 根目录下主要文件夹
1. **/bin**：普通用户可执行的基础命令（ls、cp、mv）
2. **/boot**：系统启动文件、内核，不要随便改
3. **/dev**：设备文件，硬盘、鼠标、网卡都在这里
4. **/etc（图里蓝色选中）** ⭐**最重要**
系统所有配置文件都在这。网卡配置、用户、环境变量、各种服务配置全部放 `/etc`。
> 改网络、改ssh、改系统参数基本都来这个目录。
5. **/home**：普通用户家目录，比如普通用户user，就对应 `/home/user`
6. **/lib /lib64**：系统依赖库文件，程序运行需要
7. **/media**：自动挂载U盘、光盘
8. **/mnt**：手动挂载磁盘用
9. **/opt**：第三方大型软件安装目录
10. **/proc**：虚拟目录，存放内核、进程实时信息，不占磁盘
11. **/root**：**root超级管理员的家目录**，和普通用户/home分开
12. **/run**：程序运行时临时文件，重启就清空
13. **/sbin**：管理员才能用的系统命令
14. **/srv**：服务数据目录
15. **/sys**：硬件系统信息虚拟目录
16. **/tmp**：临时文件夹，重启文件会被清理
17. **/usr**：软件程序安装目录，相当于Windows的Program Files
18. **/var**：变化的数据：日志log、缓存、数据库、网页数据

---
简单记重点：
- 改配置 → **/etc**
- 管理员文件 → **/root**
- 普通用户文件 → **/home**
- 日志、动态数据 → **/var**
- 临时文件 → **/tmp**
