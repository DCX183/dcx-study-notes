一.帮助类命令
man【命令或配置文件】
-显示说明：
NAME --命令的名称和单行描述
SYNOPSIS--怎样使用命令
DESCRIPTION--命令功能的深入讨论
EXAMPLES--怎样使用命令的例子
SEE ALSO--相关主题（通常是手册页）
help 命令
二.开关机类命令
sync将内存中的数据同步到硬盘中
poweroff关闭系统，等同于shutdown -h now
reboot重启系统，等同于shutdown -r now
shutdown【选项】时间
-h=half关机
-r=reboot重启
now 立刻关机
时间 等待多长时间关机，单位分钟
三.服务管理类命令
systemctl start 服务名 开启服务
systemctl stop 服务名 关闭服务
systemctl restart 服务名 重启
systemctl status 服务名 查看服务
systemctl --type service 查看正在运行的服务
四.文件目录类命令
pwd--显示当前工作目录的绝对路径
ls--列出目录内容
ls -a--全部的文件，连同隐藏档（开头为.的文件）一起列出
ls -l--长数据串列出，包括文件的属性与权限  =ll
cd--切换路径
cd~或cd--回到自己家的目录
cd- --回到上一次所在的目录
cd.. --回到当前目录的上一级目录
cd -P --跳转到实际物理路径，而非快捷方式
cd/ --回到系统根目录
mkdir--建立目录
mkdir -p --创建多级目录
rmdir --删除空目录
touch --创建空文件
cp --复制文件或目录
cp -r --递归复制整个文件夹
\cp--强制覆盖不提示
rm--移除文件或目录
rm -r --递归删除目录所有内容
rm -f --强制删除，不提示用户进行确认
rm -v --显示指令的详细执行过程
mv--移动文件与目录或重命名
mv oldnamefile newnamefile--重命名
mv /temp/movefile/targetfolder--移动文件
